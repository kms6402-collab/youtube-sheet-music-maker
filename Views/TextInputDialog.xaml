<Window x:Class="ScoreCap.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="텍스트 입력" Height="230" Width="420" MinHeight="230" MinWidth="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}"
        FontFamily="Segoe UI">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="입력할 텍스트" Style="{StaticResource LabelStyle}"/>
        <TextBox x:Name="TextInputBox" Grid.Row="1" FontSize="14" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="글자 크기" VerticalAlignment="Center" Margin="0,0,8,0" Style="{StaticResource LabelStyle}"/>
            <Slider x:Name="FontSizeSlider" Width="160" Minimum="10" Maximum="72" Value="28" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="취소" Margin="0,0,8,0" Click="CancelButton_OnClick"/>
            <Button Content="확인" Style="{StaticResource PrimaryButtonStyle}" Padding="16,7" IsDefault="True" Click="OkButton_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
